# Opcional: chave da API do Claude para explicações pedagógicas ricas.
# Sem isso, a ferramenta usa um comentário heurístico (funciona offline).
ANTHROPIC_API_KEY=

# Opcional: caminho do binário do Stockfish, se não estiver no PATH.
# Ex.: /usr/games/stockfish
STOCKFISH_PATH=
